apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true

  - name: Logs - Loki
    type: loki
    access: proxy
    url: http://host.docker.internal:3111
    isDefault: false
